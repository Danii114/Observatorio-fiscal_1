name: SECOP Automatizado

on:
  schedule:
    - cron: '0 11 * * *'  # todos los días
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true  # opcional, evita advertencias Node.js 20

jobs:
  run-secoppipeline:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Clonar repositorio
        uses: actions/checkout@v3

      - name: 🐍 Instalar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 📦 Instalar librerías
        run: |
          pip install pandas requests pyarrow

      - name: 🚀 Ejecutar descarga 3 meses
        run: python scripts/descarga_3mes_datos.py

      - name: 💾 Subir cambios automáticamente
        run: |
          git add . || echo "No changes to add"
          git commit -m "Actualización automática datos SECOP" || echo "No changes to commit"
          git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git
